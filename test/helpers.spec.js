/**
 * Test the helper functions.
 */

const { expect } = require('chai');
const {
    createCharacterPreview,
    createMarkerPreview,
    createWordPreview,
} = require('../lib/helpers');


///////////
// TESTS //
///////////

describe('helpers.js', () => {
    it();
});
